# Jenkins - Bitbucket - Sonarqube - Artifactory - Postgresql

#### Clone this repository and run following commands:

- cd jenkins-bitbucket-sonarqube-artifactory/
- **docker-compose up -d**

#### Docker  image versions are in ".env" file.
